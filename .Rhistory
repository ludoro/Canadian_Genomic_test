input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
input[2,4]
input[2:5,4] + input[2:5,3]
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
ratio = input[2:,4]/input[2:,5]
ratio = input[2:end,4]/input[2:end,5]
ratio = input[2,4]/[2,5]
ratio = input[2,4]/[2,5]
ratio = input[2,4] /input[2,5]
ratio = input[2:end,4] /input[2:end,5]
View(input)
View(input)
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
ratio = input[2:end,5] /input[2:end,4]
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
ratio = input[,5] /input[,4]
logratio = log(ratio)
logratio = log(-1*ratio)
logratio = log(-1*ratio)
logratio = log(-1*ratio)
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
M <- matrix(input)
ratio = M[,5] /M[,4]
View(M)
View(M)
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
df <- data.frame(input)
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
df <- data.frame(input)
View(df)
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
df <- data.frame(input)
pmfY <- apply(as.matrix.noquote(DF),5,as.numeric)
pmfY <- apply(as.matrix.noquote(df),5,as.numeric)
pmfY <- apply(as.matrix.noquote(df),as.numeric)
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
df <- data.frame(input)
pmfY <- apply(as.matrix.noquote(df),2,as.numeric)
View(pmfY)
View(pmfY)
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#df <- data.frame(input)
numrical<- apply(as.matrix.noquote(input),2,as.numeric)
numerucal[2,3]
numerical[2,3]
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
numrical <- apply(as.matrix.noquote(input),2,as.numeric)
numrical[2,4]
numrical[2,4] / numrical[2,4]
numrical[2,4] / numrical[2,5]
numrical[:,4] / numrical[:,5]
numrical[,4] / numrical[,5]
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
numrical <- apply(as.matrix.noquote(input),2,as.numeric)
x = numrical[,5] / numrical[,4]
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
numrical <- apply(as.matrix.noquote(input),2,as.numeric)
ratio = numrical[,5] / numrical[,4]
logratio = log(ratio)
View(numrical)
View(numrical)
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
numrical <- apply(as.matrix.noquote(input),2,as.numeric)
ratio = numrical[,6] / numrical[,5]
logratio = log(ratio)
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
numrical <- apply(as.matrix.noquote(input),2,as.numeric)
ratio = numrical[,5] / numrical[,4]
View(numrical)
View(numrical)
View(input)
View(input)
View(numrical)
View(numrical)
numrical <- apply(as.matrix.noquote(input),2,as.numeric)
ratio = numrical[,5] / numrical[,4]
logratio = log(abs(ratio))
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
numrical <- apply(as.matrix.noquote(input),2,as.numeric)
ratio = input[,5] / input[,4]
logratio = log(abs(ratio))
View(input)
View(input)
input[2,1]
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
plot(input[,2],logratio)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
plot(log(input[,2]),logratio)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
plot(input[,2],logratio)
ggplot(input[,2],logratio, aes(cyl, hwy)) + geom_jitter()
install.packages("ggplot2")
plot(input[,2],logratio)
ggplot(input[,2],logratio, aes(cyl, hwy)) + geom_jitter()
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
plot(input[,2],logratio)
ggplot2(input[,2],logratio, aes(cyl, hwy)) + geom_jitter()
View(input)
View(input)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
plot(input[,2],logratio)
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
install.packages("ggplot")
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
plot(input[,2],logratio)
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot2(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot2(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
ggplot2()
install.packages(
"ggplot2",
repos = c("http://rstudio.org/_packages",
"http://cran.rstudio.com")
)
ggplot2
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot2(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot2(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
library(ggplot2)
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot2(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot2(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot2(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot2(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot2(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot2(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
install.packages(ggplot2)
install.packages(ggplot)
install.packages("ggplot")
install.packages("ggplot2")
install.packages("ggplot2")
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot2(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
library(ggplot2)
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot2(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot(vector, aes(time=time, logRatio = logRatio)) + + geom_point(alpha = 0.3)
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot(vector, aes(time=time, logRatio = logRatio)) +  geom_point(alpha = 0.3)
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(time = input[,2], logRatio = logratio)
ggplot(vector, aes(x=x,y=y)) +  geom_point(alpha = 0.3)
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(x = input[,2], y = logratio)
ggplot(vector, aes(x=x,y=y)) +  geom_point(alpha = 0.3)
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(x = input[,2], y = logratio)
ggplot(vector, aes(x=x,y=y)) +  geom_point(alpha = 0.1)
ggplot(vector,aes(x=x,y=y)) + stat_binhex()
install.packages("hexbin")
library(hexbin)
ggplot(vector,aes(x=x,y=y)) + stat_binhex()
ggplot(vector,aes(x=x,y=y)) + geom_bin2d()
ggplot(vector,aes(x=l,y=y)) + geom_bin2d()
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(l = input[,2], y = logratio)
ggplot(vector, aes(x=x,y=y)) +  geom_point(alpha = 0.1)
ggplot(vector,aes(x=x,y=y)) + stat_binhex()
ggplot(vector,aes(x=l,y=y)) + geom_bin2d()
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(l = input[,2], y = logratio)
ggplot(vector, aes(x=x,y=y)) +  geom_point(alpha = 0.1)
ggplot(vector,aes(x=x,y=y)) + stat_binhex()
ggplot(vector,aes(x=x,y=y)) + geom_bin2d()
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(x = input[,2], y = logratio)
ggplot(vector, aes(x=x,y=y)) +  geom_point(alpha = 0.1)
ggplot(vector,aes(x=x,y=y)) + stat_binhex()
ggplot(vector,aes(x=x,y=y)) + geom_bin2d()
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(x = input[,2], y = logratio)
ggplot(vector, aes(x=x,y=y)) +  geom_point(alpha = 0.1)
ggplot(vector,aes(x=x,y=y)) + stat_binhex()
o3 <- ggplot(vector, aes(x, y)) +
stat_density_2d(aes(fill = ..level..), geom = 'polygon') +
scale_fill_viridis_c(name = "density") +
geom_point(shape = '.')
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(x = input[,2], y = logratio)
#ggplot(vector, aes(x=x,y=y)) +  geom_point(alpha = 0.1)
#ggplot(vector,aes(x=x,y=y)) + stat_binhex()
o3 <- ggplot(vector, aes(x, y)) +
stat_density_2d(aes(fill = ..level..), geom = 'polygon') +
scale_fill_viridis_c(name = "density") +
geom_point(shape = '.')
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(x = input[,2], y = logratio)
#ggplot(vector, aes(x=x,y=y)) +  geom_point(alpha = 0.1)
#ggplot(vector,aes(x=x,y=y)) + stat_binhex()
o3 <- ggplot(vector, aes(x, y)) +
stat_density_2d(aes(fill = ..level..), geom = 'polygon') +
scale_fill_viridis_c(name = "density") +
geom_point(shape = '.')
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(x = input[,2], y = logratio)
#ggplot(vector, aes(x=x,y=y)) +  geom_point(alpha = 0.1)
#ggplot(vector,aes(x=x,y=y)) + stat_binhex()
o3 <- ggplot(vector, aes(x, y)) +
stat_density_2d(aes(fill = ..level..), geom = 'polygon') +
scale_fill_viridis_c(name = "density") +
geom_point(shape = '.')
o3
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(x = input[,2], y = logratio)
#ggplot(vector, aes(x=x,y=y)) +  geom_point(alpha = 0.1)
#ggplot(vector,aes(x=x,y=y)) + stat_binhex()
ggplot(df, aes(x, y)) +
geom_point(alpha = 0.05)
library(ggplot2)
#I take the input from the .tsv file, it is a 15488x5 matrix
input = read.table(file = 'SCONES_test.tsv', sep = '\t', header = TRUE)
#I do the ratio element by element between last and second to last column
ratio = input[,5] / input[,4]
#I realize that the ratio may be negative as well, so I take the absolute value in order
logratio = log(abs(ratio))
#Plotting the result:
vector = data.frame(x = input[,2], y = logratio)
#ggplot(vector, aes(x=x,y=y)) +  geom_point(alpha = 0.1)
#ggplot(vector,aes(x=x,y=y)) + stat_binhex()
ggplot(vector, aes(x, y)) +
geom_point(alpha = 0.05)
ggplot(vector, aes(x, y)) +
geom_point(alpha = 0.09)
ggplot(vector, aes(x, y)) +
geom_point(alpha = 0.07)
ggplot(vector, aes(x, y)) +
stat_density_2d(aes(fill = ..density..), geom = 'raster', contour = FALSE) +
scale_fill_viridis_c() +
coord_cartesian(expand = FALSE) +
geom_point(shape = '.', col = 'white')
ggplot(vector, aes(x, y)) +
geom_point(alpha = 0.07)
ggplot(vector,aes(x=x,y=y)) + stat_binhex()
ggplot(vector, aes(x, y)) +
geom_point(alpha = 0.07)
ggplot(vector, aes(x, y)) +
stat_density_2d(aes(fill = ..density..), geom = 'raster', contour = FALSE) +
scale_fill_viridis_c() +
coord_cartesian(expand = FALSE) +
geom_point(shape = '.', col = 'white')
ggplot(vector, aes(x, y)) +
geom_point(alpha = 0.07)
ggplot(vector, aes(x, y)) +
geom_point(alpha = 0.04)
ggplot(vector,aes(x=x,y=y)) + stat_binhex()
ggplot(vector, aes(x, y)) +
geom_point(alpha = 0.07)
ggplot(vector, aes(x, y)) +
stat_density_2d(aes(fill = ..density..), geom = 'raster', contour = FALSE) +
scale_fill_viridis_c() +
coord_cartesian(expand = FALSE) +
geom_point(shape = '.', col = 'white')
ggplot(vector, aes(x, y)) +
stat_density_2d(aes(fill = ..density..), geom = 'raster', contour = FALSE) +
scale_fill_viridis_c() +
coord_cartesian(expand = FALSE) +
geom_point(shape = '.', col = 'white')
ggplot(vector,aes(x=x,y=y)) + stat_binhex()
ggplot(vector, aes(x, y)) +
geom_point(alpha = 0.04)
